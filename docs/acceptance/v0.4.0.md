# v0.4.0 验收报告：设计真源 Catalog 2.0

| 项 | 内容 |
|---|---|
| 版本 | `v0.4.0` |
| OpenSpec | `ai-form-design-truth-catalog`（`ready`） |
| Evidence Manifest | `.deliveryguard/acceptance/v0.4.0/evidence.json` |
| 结论 | **acceptance passed**（本地 agent/static + Playwright 抽样；release 仍 pending） |

## 范围

- DesignTruthGraph 多真源编译（widgetsConfig + propertyRegister + 219 editors + 渲染/联动/identity/扩展边界 policy）
- 统一 Validator + IntentGate（labelWidth number、size enum、诚实 summary）
- NL 归一、label/parentScope 定位、tab 批量、容器 refine 子集
- generate / refine / Excel 三路径同源 Catalog
- 50×(type,prop) 抽样 parity（`catalog-sample-parity`）
- loadFormJson 前 `preApplyFormJsonGate` 结构门闩（契约层）

## 验证命令

```bash
cd agent
npm run typecheck
npm run catalog:check
npm run acceptance:cases
npm run smoke

cd ../e2e
npm test -- tests/ai-form-v040.spec.ts
```

## 非目标（仍不在本版 release 范围）

- 运行时 custom widget（card/alert）静态全覆盖
- 生产部署锚点
- 事件脚本执行主路径
