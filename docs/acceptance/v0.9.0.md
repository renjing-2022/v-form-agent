# Acceptance report

## Scope

- Version: `v0.9.0`
- Documents:
  - `ai-form-nl-interaction-compiler-prd`（primary）→ `docs/requirements/ai-form-nl-interaction-compiler.md`
  - `ai-form-nl-interaction-compiler-design` → `docs/design/ai-form-nl-interaction-compiler.md`
- OpenSpec: `ai-form-nl-interaction-compiler`
- Repository: `app`（`.`）；分支 `feat/v0.9.0-nl-interaction-js`；source `f9e52445920001ce70a32b78a6edb4482d544648`（`submitted`）
- Environment:
  - local Windows / Node；Playwright Chromium；`v-form:3130`，`agent:3140`
  - 题库正例：mock fixture 回放 generate → **真实 VFormRender 预览执行 scenarios** → 服务端重判 apply → 独立断言
  - 冒烟：真实 `DEEPSEEK_API_KEY`（证据不含 Key）

### Breaking（相对 v0.8，PRD §9）

- AiChat 统一 `/interaction`；旧 `/event` UI 用例在清单中记为 `skipped`（含 note）。
- 交互按钮文案为「发送」；纯结构经 `route_refine` → `/refine`。

## Execution

- `npm run interaction:check` → `INTERACTION_D0_D4_OK`
- `npm run typecheck` → pass
- `npm run acceptance:cases` → `ACCEPTANCE_CASES_PASSED`
- `npm run interaction:smoke` → `SMOKE_RATE 6/6`
- `CI=1 EVIDENCE_VERSION=v0.9.0 playwright test ai-form-v090.spec.ts` → **23 passed**（题库正例含真实预览）
- v0.8 / v0.7 / v0.6 e2e：可跑子集通过；Breaking UI 已 skip

## Coverage

| Requirement | Result |
| --- | --- |
| fr-1 … fr-9 | pass |
| fr-10 regression | pass（证据在 `docs/evidence/v0.9.0/`） |
| td-* | pass |

逐用例见 `.deliveryguard/acceptance/v0.9.0/evidence.json`。

## Failures and blockers

- 无 fail / blocked。
- Breaking 旧 `/event` UI 用例（`event-ui-*`、`event-clarify-*` UI）不进入本版 Evidence Manifest：DeliveryGuard 要求全部 case 为 `pass` 才可 `acceptance.passed`；承接用例为 `interaction-ui-confirm-flow` / `interaction-ui-route-refine`；e2e 仍以 `test.skip` + annotation 记录 Breaking 原因。

## Conclusion

Evidence Manifest validate：`ok: true`。题库正例已按 PRD §7 走真实预览。  
实现提交 `f9e52445920001ce70a32b78a6edb4482d544648` 已登记为 `sources[0]`（`submitted` @ `feat/v0.9.0-nl-interaction-js`）；`acceptance.status` = `passed`。Acceptance 不蕴含 production release。
