# Acceptance report

## Scope

- Version: `v0.8.0`
- Documents:
  - `ai-form-event-js-execution-prd`（primary）→ `docs/requirements/ai-form-event-js-execution.md`
  - `ai-form-event-js-execution-design` → `docs/design/ai-form-event-js-execution.md`
- OpenSpec: `ai-form-event-js-execution`
- Repository: `app`（`.`），branch `feat/v0.8.0-event-js-execution`，base `9691f03052f0d400c908ef26a9251fcd60f4c1b9`
- Environment:
  - local Windows / Node；Playwright Chromium；`v-form:3130`，`agent:3140`
  - `AGENT_ALLOW_MOCK=1`：生成走确定性模板，事件执行走真实 VFormRender
  - 无登录；设计器本地画布 fixture

### 本版本实际交付范围（收窄声明）

v0.8 交付的是 **事件 JS 的安全写入与真实执行闸 + 少量模板化交互**：

- `/event` 的 clarify → generate(code_preview) → apply(applied/draft) 状态机；
- 事件 JS 静态护栏（网络、动态执行、DOM、定时器、未知字段）；
- 只有在真实 VFormRender 中按 examples 执行、且服务端以 `actual` 对照 `expect` 重判全部一致时才允许写入画布；
- 模板覆盖：值变更求和、值变更显隐/禁用、按钮/生命周期/子表增行写入字面量、提交前「X 不能小于/大于 N」类校验。

v0.8 **不**支持任意自然语言描述的页面交互（如 tab 下一页+校验+失败定位）。该目标由 v0.9（LLM → InteractionPlan → 确定性编译）承担。

## 验证真实性修复

首轮验收中发现验证不真实，已修复并重跑全部用例：

| 问题 | 修复 | 证明用例 |
| --- | --- | --- |
| 7 条 e2e 手写 `executionReport.pass=true` | e2e 真实输入/点击/装载/增行/validateForm 后读状态，报告由观测与 examples 比对生成 | event-compute-onchange-apply 等 7 条 |
| 前端 runner 对 hidden/disabled/valid 直接回填期望值；onClick 不触发 | runner 真实触发并从控件 `field.options`、`validateForm` 读取；读不到判失败 | event-ui-apply-gate；event-ui-verify-rejects-mismatch |
| AiChat 隐藏预览用独立 `createApp`，全局控件未注册，字段不渲染 | 复用设计器 appContext 挂载 | event-ui-apply-gate |
| 服务端只信任 `ok` 标记 | 服务端逐键以 `actual` 对照 `expect` 重判 | event-apply-forged-ok-rejected |
| onClick/onFormMounted 生成 `setFieldValue('zf', value)`（`value` 未定义）；onFormValidate 无规则 | 按 eventKey 分派；校验规则从描述编译，含糊返回 422 | event-codegen-dispatch-by-event-key |

## Execution

全部证据在干净的实现提交 `cd81e52d26dd24cb18623a3ee21e24e94ac6f8d9` 上重新生成（证据文件 `sourceRevision` 无 dirty 标记）：

- `npm run catalog:check` → `DESIGN_TRUTH_CATALOG_SYNC_OK`
- `npm run event-shape:check` → `EVENT_SHAPE_SYNC_OK shapes=49`
- `npm run typecheck`（agent）→ pass；`npx tsc --noEmit`（e2e）→ pass
- `npm run acceptance:cases` → `ACCEPTANCE_CASES_PASSED`（含 v0.2–v0.7 回归段；旧版本证据文件未改写）
- `EVIDENCE_VERSION=v0.8.0 npm run test:v080` → **9 passed**
- `npx playwright test tests/ai-form-v070.spec.ts --reporter=line` → **3 passed**（回归，仅终端输出，不改写 v0.7 证据）
- `deliveryguard acceptance validate` → `ok: true`

## Coverage

| Requirement | Cases | Result |
| --- | --- | --- |
| fr-1-constrained-generation | generate preview；公式优先；按事件类型生成 | pass |
| fr-2-static-guard | network；eval/DOM/timer；unknown-field | pass |
| fr-3-real-render-apply-gate | 无报告拒绝；失败报告 draft；伪造 ok 重判；7 条真实触发 e2e | pass |
| fr-4-merge-overwrite | applied 合入；手写覆盖策略；refine 禁写 | pass |
| fr-5-catalog-write-policy | allowlist；dialog allowed/danger；strict sweep | pass |
| fr-6-frontend-flow | AiChat 全链路写入；期望不符拒绝写入；真实预览计算 | pass |
| td-event-state-machine | generate；无/失败/伪造报告；成功 apply | pass |
| td-event-js-guard | 网络、动态执行、DOM、定时器、未知字段 | pass |
| td-preview-execution | 9 条 Playwright 真实执行用例 | pass |
| td-merge-catalog-routing | merger、覆盖策略、event/refine 分流 | pass |
| td-regression-contracts | v0.7；v0.6；strict sweep；no-secret | pass |

逐用例映射见 `.deliveryguard/acceptance/v0.8.0/evidence.json`，26 个用例均有 `docs/evidence/v0.8.0/` 下的证据文件且结果为 `pass`，无 fail / blocked / skipped。

## Known limits

- 生成为确定性模板（mock 模式），不构成 LLM 质量证据；模板外的描述会降级或返回 422/need_clarification。
- 值变更显隐/禁用为无条件动作（改动即隐藏/禁用），不解析「当 X=0 时」之类条件。
- AiChat 的交互意图识别仍是关键词判断；复杂描述可能被路由到 `/refine`（v0.9 处理）。
- 源码仅为本地提交（`status: local`），未推送、未合并、未部署。

## Conclusion

Evidence Manifest 已校验，26/26 用例 pass，功能验收无失败。

版本验收状态保持 **pending**：`deliveryguard check` 要求 source 至少为 `submitted` 才能记为 passed（`acceptance.before-source`），当前实现提交仅为本地（`local`）。推送分支或提交评审后，将 source 更新为 `submitted`，即可在同一提交的证据上把验收记为 passed。

Acceptance 不代表生产发布；当前未记录部署或 release 事实。

Acceptance 不代表生产发布；当前未记录部署或 release 事实。
