# v0.1.0 本地验证记录

记录时间：2026-08-27  
环境：Windows，Node v22

## 已执行

| 用例 | 命令/方式 | 结果 |
|---|---|---|
| Agent 单元冒烟 | `cd agent && npm run smoke` | `SMOKE_PASSED` |
| TypeScript | `cd agent && npm run typecheck` | 通过 |
| E2E 验收 | `cd e2e && npm test` | 3 passed |
| 健康检查 | `GET /health` | `ok: true` |
| 文本生成 API | `POST /api/agent/v1/generate` mode=text | 返回 formJson |
| Excel 生成 API | multipart 上传合成 xlsx | 200，含 radio |
| 空文件负例 | multipart 空 file | 400 |
| DeliveryGuard manifest | `deliveryguard acceptance validate` | 通过 |
| DeliveryGuard check | `deliveryguard check` | passed（阶段仍为 specified） |

## 验收证据位置

- Manifest: `.deliveryguard/acceptance/v0.1.0/evidence.json`
- 用例证据: `docs/evidence/v0.1.0/`
- 报告: `docs/acceptance/v0.1.0.md`

## 说明

- 全部 12 个 acceptance case 当前为 `pass`
- 版本 `acceptance.status` 仍为 `blocked`，因 `sources` 未登记
- E2E 使用 Playwright + mock Agent（3130/3140），不调用付费 DeepSeek
