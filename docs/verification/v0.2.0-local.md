# v0.2.0 本地验证

| 项 | 结果 |
|---|---|
| `cd agent && npm run typecheck` | 通过 |
| `cd agent && npm run smoke` | `SMOKE_PASSED`（含 string-target coerce） |
| `cd agent && npm run acceptance:cases` | `ACCEPTANCE_CASES_PASSED` → `docs/evidence/v0.2.0/*.txt` |
| 人工问题修复 | 模型 `targets: string[]` 导致 Zod 500 → 已归一化为 `{id,name}`，契约失败改 422 |
| Playwright UI | **本轮不做**（按请求 deferred） |
| DeliveryGuard Manifest / source | **未登记为 passed / implemented** |

## 人工复测建议

1. 确认 `agent` 的 `npm run dev` 已热更新（或重启一次）。
2. 空画布生成并「应用到设计器」。
3. 再输入「增加两个 tab：基本信息 / 评估题目」→「优化当前表」→ 应返回成功 summary（不再 500）。
4. 确认后整表覆盖应用。
