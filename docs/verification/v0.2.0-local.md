# v0.2.0 本地验证

| 项 | 结果 |
|---|---|
| 目标版本 | `v0.2.0`（OpenSpec `ai-form-multiturn-refine` / `applied`） |
| 实现 commit | `26bbb3c`（多轮 refine 实现） |
| Source 登记 | `main@3a45a51` / **`merged`**；发布钉点 `d872dab` / tag `v0.2.0` |
| `cd agent && npm run typecheck` | 通过 |
| `cd agent && npm run smoke` | `SMOKE_PASSED`（generate 回归 + refine 两轮 + string-target coerce + 非法类型拒绝） |
| `cd agent && npm run acceptance:cases` | `ACCEPTANCE_CASES_PASSED` |
| `cd e2e && npm test` | **9 passed**（`EVIDENCE_VERSION=v0.2.0`） |
| Evidence Manifest | `.deliveryguard/acceptance/v0.2.0/evidence.json`（10 cases / 全 pass） |
| Acceptance report | `docs/acceptance/v0.2.0.md` |
| `deliveryguard acceptance validate` | ok（`--version=.deliveryguard/versions/v0.2.0.json`） |
| `deliveryguard check` | ok |
| `deliveryguard status` | `v0.2.0` → **`released`**（source=`merged`；acceptance=`passed`；production tag `v0.2.0`） |

## Playwright cases（本版）

| case-id | 断言要点 |
|---|---|
| `text-generate-apply` | `/api/agent/v1/generate` 经代理 200；显式应用后画布有控件 |
| `refine-structure-tab` | `/refine` 200；应用后出现 `.tab-container` |
| `refine-multiturn-apply` | 两轮 refine 均 200 并应用；会话消息仍可见 |
| `refine-options-formula` | 选项改写和/或「总分」可见 |
| `refine-reject-keeps-canvas` | 强制 refine 422；错误可见；画布控件数不变 |
| `refine-text-style-policy` | 纯样式诉求 → 422；错误含样式/CSS；画布文案与控件数不变 |
| `refine-explicit-text-apply` | 明确改标题 → 200 并应用；画布出现新标题 |
| `ui-excel-canvas-apply-e2e` | Excel 回归（留存证据） |
| `ui-invalid-upload-preserves-canvas` | 空 Excel 回归（留存证据） |

## 发布事实

| 项 | 值 |
|---|---|
| production deployment | `succeeded` @ `d872dab` |
| deployment anchor | https://github.com/renjing-2022/v-form-agent/tree/v0.2.0 |
| release | `published` |
| release anchor | https://github.com/renjing-2022/v-form-agent/releases/tag/v0.2.0 |
